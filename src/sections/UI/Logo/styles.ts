import styled from "styled-components";

export const Logo = styled.span`
  .logo-home {
    font-size: 4rem;
    font-style: italic;
    cursor: pointer;
  }

  @media (max-width: 700px){
    font-size: 3rem;
  }
`;
